import { ArrowRightIcon, ChevronRightIcon, CrossCircledIcon } from "@radix-ui/react-icons";
import { Callout, Text, Heading, Link, Strong, Box, Flex } from "@radix-ui/themes";

const UnknownPage = () => (
    <Callout.Root>
        <Callout.Icon>
            <CrossCircledIcon />
        </Callout.Icon>
        <Callout.Text>
            <Flex>
                <Strong>Unknown page</Strong>
                <Box px="2" mt="0.8"> <ArrowRightIcon /> </Box>
                <Text>Go back to the <Link href="#">home page</Link>.</Text>
            </Flex>
        </Callout.Text>
    </Callout.Root>)

export default UnknownPage

